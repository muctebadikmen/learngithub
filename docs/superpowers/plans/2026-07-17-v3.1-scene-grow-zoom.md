# v3.1 Scene-grow + Meaningful Zoom — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Timeline artık sıkışmıyor; sahne commit sayısına göre yatayda büyüyor; zoom o büyük sahnede anlamlı gezinme aracı oluyor (uzaklaştır=hepsini gör, yakınlaştır=oku).

**Architecture:** Yeni saf `src/sceneLayout.ts` modülü commit sayılarından panel genişliklerini + toplam sahne boyutunu hesaplar. `Scene.tsx` bu layout'u panel rect'leri, GitHub `translate` grubu ve `<svg>` genişliği için kullanır; `timelineSlots` sabit boşluk (`GAP`) kullanır. `App.tsx` `useZoom`'a dinamik `sceneW` verir. `useZoom.ts` daha düşük `MIN_SCALE` + "kullanıcı dokunmadıysa büyümede yeniden çerçevele" davranışı.

**Tech Stack:** React 19 + TypeScript + Vite + Tailwind, Vitest, oxlint.

## Global Constraints

- Turkish-only UI metni; mevcut kelime/emoji tonu korunur.
- Sıfır/az-commit durumunda görüntü **birebir mevcut geometriye** düşmeli (1280×720, paneller x=24 ve x=800). Regresyon yok.
- `SCENE_H` sabit 720; yalnızca yatay büyüme.
- Dark tema varsayılan; hiçbir renk/tema değişmiyor.
- DRY / YAGNI / TDD / sık commit. Gereksiz soyutlama yok.

---

## File Structure

- **Create** `src/sceneLayout.ts` — saf layout matematiği + `GAP`, `SCENE_H` sabitleri. Bağımlılık: yalnızca `ModelState` tipi.
- **Create** `tests/sceneLayout.test.ts` — layout birim testleri.
- **Modify** `src/Scene.tsx` — `timelineSlots` sabit GAP; panel rect/repo kutusu/GitHub translate/`<svg>` genişliği layout'tan; `SCENE_W`/`SCENE_H` sabitleri kaldırılır (`SCENE_H` `sceneLayout`'tan gelir).
- **Modify** `src/App.tsx` — `useZoom(sceneLayout(model).sceneW, SCENE_H)`; `SCENE_W` importu kaldırılır.
- **Modify** `src/useZoom.ts` — `MIN_SCALE` 0.5→0.12; büyüme-refit `adjustedRef` ile korunur.
- **Modify** `tests/zoom.test.ts` — MIN'e bağlı sabitler + geniş-sahne-sığar testi.

---

## Task 1: `sceneLayout` pure module

**Files:**
- Create: `src/sceneLayout.ts`
- Test: `tests/sceneLayout.test.ts`

**Interfaces:**
- Consumes: `ModelState` (`src/model.ts`) — alanlar: `commits: Commit[]`, `pushedIds: string[]`, `remoteExtra: Commit[]`; her `Commit` `x: number`.
- Produces:
  - `export const GAP = 112`
  - `export const SCENE_H = 720`
  - `export type SceneLayout = { gap: number; computerPanelW: number; computerPanelR: number; repoBoxW: number; ghPanelX: number; ghPanelW: number; dx: number; sceneW: number; sceneH: number }`
  - `export function sceneLayout(s: ModelState): SceneLayout`

- [ ] **Step 1: Write the failing test**

`tests/sceneLayout.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { commit, createRepo, initialModel, push, type ModelState } from '../src/model'
import { GAP, sceneLayout, SCENE_H } from '../src/sceneLayout'

// createRepo => 1 commit (x=0); each commit adds one (x = count-1).
const withCommits = (n: number): ModelState => {
  let s = createRepo(initialModel())
  for (let i = 1; i < n; i++) s = commit(s, `c${i}`)
  return s
}

describe('sceneLayout', () => {
  it('falls back to the exact current geometry when empty', () => {
    const l = sceneLayout(initialModel())
    expect(l).toMatchObject({
      computerPanelW: 744,
      computerPanelR: 768,
      repoBoxW: 696,
      ghPanelX: 800,
      ghPanelW: 456,
      dx: 0,
      sceneW: 1280,
      sceneH: SCENE_H,
      gap: GAP,
    })
  })

  it('stays at base size for a small history', () => {
    // maxLocalX 4 => 116 + 4*112 + 72 = 636 < 768, still base.
    expect(sceneLayout(withCommits(5)).sceneW).toBe(1280)
  })

  it('grows the computer panel and the scene once commits pass the base width', () => {
    const l = sceneLayout(withCommits(12)) // maxLocalX = 11
    expect(l.computerPanelR).toBe(116 + 11 * GAP + 72) // 1420
    expect(l.computerPanelW).toBe(l.computerPanelR - 24)
    expect(l.sceneW).toBeGreaterThan(1280)
  })

  it('keeps the GitHub panel to the right of the computer panel (no overlap)', () => {
    for (const n of [1, 5, 12, 25]) {
      const l = sceneLayout(withCommits(n))
      expect(l.ghPanelX).toBe(l.computerPanelR + 32)
      expect(l.dx).toBe(l.ghPanelX - 800)
      expect(l.sceneW).toBe(l.ghPanelX + l.ghPanelW + 24)
    }
  })

  it('is monotonic in commit count', () => {
    expect(sceneLayout(withCommits(20)).sceneW).toBeGreaterThan(sceneLayout(withCommits(10)).sceneW)
  })

  it('widens the GitHub panel when the cloud has many commits', () => {
    const local = sceneLayout(withCommits(12)).ghPanelW // nothing pushed => 456
    const pushed = sceneLayout(push(withCommits(12))).ghPanelW // 12 cloud commits
    expect(local).toBe(456)
    expect(pushed).toBeGreaterThan(456)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/sceneLayout.test.ts`
Expected: FAIL — `sceneLayout`/`GAP`/`SCENE_H` not exported (module `src/sceneLayout.ts` missing).

- [ ] **Step 3: Write minimal implementation**

`src/sceneLayout.ts`:

```ts
import type { ModelState } from './model'

// Fixed horizontal spacing between timeline nodes. Never compresses, so
// labels can't collide no matter how many commits there are — the scene
// grows sideways instead (useZoom then frames/inspects it).
export const GAP = 112

// The scene only grows horizontally; height is fixed.
export const SCENE_H = 720

export type SceneLayout = {
  gap: number
  computerPanelW: number
  computerPanelR: number
  repoBoxW: number
  ghPanelX: number
  ghPanelW: number
  dx: number
  sceneW: number
  sceneH: number
}

const maxX = (cs: { x: number }[]): number => (cs.length ? Math.max(...cs.map((c) => c.x)) : 0)

// Panel geometry derived from commit counts. Base case (empty/small history)
// reproduces the original fixed layout: computer panel x=24..768, GitHub
// panel x=800..1256, sceneW 1280.
export function sceneLayout(s: ModelState): SceneLayout {
  // Computer side: rightmost local node + label/📍pin padding (72).
  const computerContentR = 116 + maxX(s.commits) * GAP + 72
  const computerPanelR = Math.max(768, computerContentR)
  const computerPanelW = computerPanelR - 24
  const repoBoxW = computerPanelW - 48

  // GitHub side, authored in its original 800-based local space then shifted
  // right by dx. cloud = pushed local commits + teammate's remoteExtra.
  const cloud = [...s.commits.filter((c) => s.pushedIds.includes(c.id)), ...s.remoteExtra]
  const ghContentRLocal = Math.max(1256, 852 + maxX(cloud) * GAP + 60)
  const ghPanelW = ghContentRLocal - 800
  const ghPanelX = computerPanelR + 32
  const dx = ghPanelX - 800

  const sceneW = ghPanelX + ghPanelW + 24

  return { gap: GAP, computerPanelW, computerPanelR, repoBoxW, ghPanelX, ghPanelW, dx, sceneW, sceneH: SCENE_H }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/sceneLayout.test.ts`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/sceneLayout.ts tests/sceneLayout.test.ts
git commit -m "feat(layout): sceneLayout — content-driven panel/scene sizing"
```

---

## Task 2: Wire fixed spacing + dynamic sizing into Scene & App

**Files:**
- Modify: `src/Scene.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `sceneLayout`, `GAP`, `SCENE_H` from `./sceneLayout` (Task 1).
- Produces: `Scene` still `({ state, labeled }) => JSX`. `SCENE_W`/`SCENE_H` no longer exported from `Scene.tsx`.

**Note:** Scene ve App aynı anda değişmeli — `SCENE_H`/`SCENE_W` `Scene`'den `sceneLayout`'a taşındığı için ikisi birlikte derlenir. Bu görevin "yeşil"i: `npx vitest run` (49 test) + `npm run build` + `npx oxlint` hepsi temiz.

- [ ] **Step 1: `Scene.tsx` — import layout, drop old constants**

`src/Scene.tsx` en üstteki importlara ekle (mevcut `./model` importunun altına):

```ts
import { GAP, sceneLayout, SCENE_H } from './sceneLayout'
```

`src/Scene.tsx` içindeki mevcut satırları:

```ts
export const SCENE_W = 1280
export const SCENE_H = 720
```

ile değiştir → **tamamen sil** (artık `sceneLayout`'tan geliyor).

- [ ] **Step 2: `Scene.tsx` — fixed-gap `timelineSlots`**

Mevcut `timelineSlots` fonksiyonunu:

```ts
function timelineSlots(commits: Commit[], startX: number, endX: number, r: number, laneYFn: (laneIdx: number) => number): Slot {
  const maxX = commits.length ? Math.max(...commits.map((c) => c.x)) : 0
  const gap = maxX > 0 ? Math.min(120, (endX - startX) / maxX) : 0
  return {
    px: (x: number) => startX + x * gap,
    r,
    laneY: laneYFn,
  }
}
```

şununla değiştir:

```ts
// Fixed spacing (GAP): nodes never compress, so labels never collide — the
// scene widens instead (see sceneLayout).
function timelineSlots(startX: number, r: number, laneYFn: (laneIdx: number) => number): Slot {
  return {
    px: (x: number) => startX + x * GAP,
    r,
    laneY: laneYFn,
  }
}
```

- [ ] **Step 3: `Scene.tsx` — update the two `timelineSlots` call sites**

`GitHubSide` içinde:

```ts
const slot = timelineSlots(cloudCommits, 852, 1206, 12, cloudLaneY)
```
→
```ts
const slot = timelineSlots(852, 12, cloudLaneY)
```

`Scene` içinde:

```ts
const slot = timelineSlots(state.commits, 116, 700, 18, laneY)
```
→
```ts
const slot = timelineSlots(116, 18, laneY)
```

- [ ] **Step 4: `Scene.tsx` — `GitHubSide` accepts a shift and widened panel**

`GitHubSide` imzasını ve panel'e bağlı iki sabit genişliği güncelle. İmza:

```ts
function GitHubSide({ state, labeled }: { state: ModelState; labeled: boolean }) {
```
→
```ts
function GitHubSide({ state, labeled, panelW }: { state: ModelState; labeled: boolean; panelW: number }) {
```

`GitHubSide` içindeki iki genişlik referansını panele göre esnet:
- Header pill (repo bar) sabit kalır (sol hizalı, 416) — DOKUNMA.
- "{n} commit" sağ-hizalı metnini panel sağ kenarına sabitle. Şu satır:

```ts
<text x={1218} y={153} fontSize={14} fill="var(--ink-faint)" textAnchor="end">
```
→
```ts
<text x={800 + panelW - 22} y={153} fontSize={14} fill="var(--ink-faint)" textAnchor="end">
```

Ayrıca repo header pill genişliğini panele uydur (çok geniş panelde tutarlı görünsün):

```ts
<rect x={824} y={120} width={416} height={54} rx={12} fill="var(--pill-bg)" stroke="var(--chip-stroke)" />
```
→
```ts
<rect x={824} y={120} width={panelW - 48} height={54} rx={12} fill="var(--pill-bg)" stroke="var(--chip-stroke)" />
```

- [ ] **Step 5: `Scene.tsx` — `Scene()` uses layout for panels, GitHub translate, svg size**

`Scene` fonksiyonunun gövdesini güncelle. Mevcut başlangıç:

```ts
export function Scene({ state, labeled = false }: { state: ModelState; labeled?: boolean }) {
  const slot = timelineSlots(116, 18, laneY)
  return (
    <svg
      viewBox={`0 0 ${SCENE_W} ${SCENE_H}`}
      width={SCENE_W}
      height={SCENE_H}
      className="block max-w-none"
      role="img"
      aria-label="GitHub görselleştirmesi"
    >
      {/* computer panel */}
      <rect x={24} y={24} width={744} height={672} rx={20} fill="var(--panel)" stroke="var(--panel-stroke)" />
      {/* github panel */}
      <rect x={800} y={24} width={456} height={672} rx={20} fill="var(--panel-2)" stroke="var(--panel-stroke)" />
```

şununla değiştir:

```ts
export function Scene({ state, labeled = false }: { state: ModelState; labeled?: boolean }) {
  const slot = timelineSlots(116, 18, laneY)
  const { computerPanelW, repoBoxW, ghPanelW, dx, sceneW, sceneH } = sceneLayout(state)
  return (
    <svg
      viewBox={`0 0 ${sceneW} ${sceneH}`}
      width={sceneW}
      height={sceneH}
      className="block max-w-none"
      role="img"
      aria-label="GitHub görselleştirmesi"
    >
      {/* computer panel */}
      <rect x={24} y={24} width={computerPanelW} height={672} rx={20} fill="var(--panel)" stroke="var(--panel-stroke)" />
      {/* github panel (shifted right by dx as the computer panel grows) */}
      <g transform={`translate(${dx}, 0)`}>
        <rect x={800} y={24} width={ghPanelW} height={672} rx={20} fill="var(--panel-2)" stroke="var(--panel-stroke)" />
      </g>
```

- [ ] **Step 6: `Scene.tsx` — widen the repo dashed box**

`state.hasRepo` bloğundaki repo çerçeve kutusu:

```ts
<rect x={48} y={92} width={696} height={580} rx={16} fill="none" stroke="var(--chip-stroke)" strokeWidth={1.5} strokeDasharray="10 8" />
```
→
```ts
<rect x={48} y={92} width={repoBoxW} height={580} rx={16} fill="none" stroke="var(--chip-stroke)" strokeWidth={1.5} strokeDasharray="10 8" />
```

- [ ] **Step 7: `Scene.tsx` — wrap `GitHubSide` in the same shift + pass panelW**

Mevcut:

```ts
<GitHubSide state={state} labeled={labeled} />
```
→
```ts
<g transform={`translate(${dx}, 0)`}>
  <GitHubSide state={state} labeled={labeled} panelW={ghPanelW} />
</g>
```

- [ ] **Step 8: `Scene.tsx` — death scrim covers the widened panel**

`state.laptopDead` bloğu. Scrim ve metinleri panel genişliğine göre ortala:

```ts
{state.laptopDead && (
  <g>
    <rect x={24} y={24} width={744} height={672} rx={20} fill="var(--scrim)" opacity={0.86} />
    <text x={396} y={330} fontSize={72} textAnchor="middle">
      💀
    </text>
    <text x={396} y={396} fontSize={26} fontWeight={800} fill="var(--danger-hover)" textAnchor="middle">
      bilgisayar çöktü
    </text>
    <text x={396} y={430} fontSize={17} fill="var(--ink-muted)" textAnchor="middle">
      buradaki her şey gitti…
    </text>
  </g>
)}
```
→
```ts
{state.laptopDead && (
  <g>
    <rect x={24} y={24} width={computerPanelW} height={672} rx={20} fill="var(--scrim)" opacity={0.86} />
    <text x={24 + computerPanelW / 2} y={330} fontSize={72} textAnchor="middle">
      💀
    </text>
    <text x={24 + computerPanelW / 2} y={396} fontSize={26} fontWeight={800} fill="var(--danger-hover)" textAnchor="middle">
      bilgisayar çöktü
    </text>
    <text x={24 + computerPanelW / 2} y={430} fontSize={17} fill="var(--ink-muted)" textAnchor="middle">
      buradaki her şey gitti…
    </text>
  </g>
)}
```

- [ ] **Step 9: `App.tsx` — feed dynamic width to useZoom**

`src/App.tsx` import satırı:

```ts
import { Scene, SCENE_H, SCENE_W } from './Scene'
```
→
```ts
import { Scene } from './Scene'
import { sceneLayout, SCENE_H } from './sceneLayout'
```

`useZoom` çağrısı:

```ts
const zoom = useZoom(SCENE_W, SCENE_H)
```
→
```ts
const zoom = useZoom(sceneLayout(model).sceneW, SCENE_H)
```

(`sceneLayout` saf ve ucuz; `Scene` kendi layout'unu ayrıca hesaplar — prop threading yok, iki taraf da `state`'ten türetir.)

- [ ] **Step 10: Verify green**

Run: `npx vitest run`
Expected: PASS — 49 tests (47 mevcut + Task 1'in 6'sı; toplam ekrana göre). En az: hiçbir test kırılmadı.

Run: `npm run build`
Expected: temiz derleme, hata yok.

Run: `npx oxlint`
Expected: temiz.

- [ ] **Step 11: Commit**

```bash
git add src/Scene.tsx src/App.tsx
git commit -m "feat(canvas): grow computer/GitHub panels with history, fixed node spacing"
```

---

## Task 3: Meaningful zoom range + smart re-framing

**Files:**
- Modify: `src/useZoom.ts`
- Modify: `tests/zoom.test.ts`

**Interfaces:**
- Consumes: nothing new.
- Produces: `MIN_SCALE = 0.12`; `useZoom` yalnızca kullanıcı elle ayarlamadıysa (`adjustedRef` false) içerik büyüyünce yeniden çerçeveler.

- [ ] **Step 1: Update zoom tests to the new MIN + add wide-scene-fits test**

`tests/zoom.test.ts` içinde `clampScale` describe bloğu:

```ts
describe('clampScale', () => {
  it('clamps below MIN to 0.5', () => {
    expect(clampScale(0)).toBe(0.5)
    expect(clampScale(0.1)).toBe(0.5)
  })
```
→
```ts
describe('clampScale', () => {
  it('clamps below MIN to 0.12', () => {
    expect(clampScale(0)).toBe(0.12)
    expect(clampScale(0.05)).toBe(0.12)
  })
```

"clamps to MIN_SCALE for a tiny container" testi:

```ts
  it('clamps to MIN_SCALE for a tiny container', () => {
    const result = fitTransform(50, 50, 1280, 720)
    expect(result.scale).toBe(0.5)
  })
```
→
```ts
  it('clamps to MIN_SCALE for a tiny container', () => {
    const result = fitTransform(50, 50, 1280, 720)
    expect(result.scale).toBe(0.12)
  })

  it('fits a very wide scene below the old 0.5 floor (zoom-out now shows everything)', () => {
    // 2600-wide scene in a ~900px container needs ~0.33 — impossible under the
    // old MIN_SCALE 0.5, possible now.
    const result = fitTransform(900, 700, 2600, 720)
    const expectedScale = clampScale(Math.min(900 / 2600, 700 / 720) * 0.94)
    expect(result.scale).toBeCloseTo(expectedScale, 10)
    expect(result.scale).toBeLessThan(0.5)
    expect(result.scale).toBeGreaterThan(0.12)
  })
```

"uses the width ratio when width is the binding dimension" testindeki eski MIN yorumunu ve eşik iddiasını güncelle:

```ts
    // width/content ratio (700/1280 = 0.5469) is smaller than
    // height/content ratio (900/720 = 1.25), so width binds. Raw scale
    // (0.5469 * 0.94 ≈ 0.514) stays above MIN_SCALE, so this exercises the
    // unclamped width-bound path, not the min clamp.
    const result = fitTransform(700, 900, 1280, 720)
    const expectedScale = clampScale((700 / 1280) * 0.94)
    expect(expectedScale).toBeGreaterThan(0.5)
```
→
```ts
    // width/content ratio (700/1280 = 0.5469) is smaller than
    // height/content ratio (900/720 = 1.25), so width binds. Raw scale
    // (0.5469 * 0.94 ≈ 0.514) stays above MIN_SCALE (0.12), so this exercises
    // the unclamped width-bound path, not the min clamp.
    const result = fitTransform(700, 900, 1280, 720)
    const expectedScale = clampScale((700 / 1280) * 0.94)
    expect(expectedScale).toBeGreaterThan(0.12)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/zoom.test.ts`
Expected: FAIL — `clampScale(0)` hâlâ 0.5 döndürüyor (impl 0.12 değil); yeni geniş-sahne testi de 0.5 tabanına takılıp `toBeLessThan(0.5)` başarısız.

- [ ] **Step 3: Lower `MIN_SCALE`**

`src/useZoom.ts`:

```ts
const MIN_SCALE = 0.5
```
→
```ts
const MIN_SCALE = 0.12
```

- [ ] **Step 4: Guard the growth re-fit with `adjustedRef`**

`src/useZoom.ts` içindeki ilk framing effect'i:

```ts
  // Initial framing: fit before paint so there's no visible identity flash.
  useLayoutEffect(() => {
    fitToContainer()
  }, [fitToContainer])
```
→
```ts
  // Initial framing + re-frame when the content grows — but only while the
  // user hasn't manually panned/zoomed since the last fit (adjustedRef false).
  // So history growth keeps everything in view automatically, yet never yanks
  // a view the user has adjusted (⤢ re-fits on demand). fitToContainer's deps
  // include contentW, so this re-runs whenever the scene widens.
  useLayoutEffect(() => {
    if (!adjustedRef.current) fitToContainer()
  }, [fitToContainer])
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run tests/zoom.test.ts`
Expected: PASS.

- [ ] **Step 6: Full green**

Run: `npx vitest run`
Expected: PASS (tüm testler).

Run: `npm run build` → temiz. `npx oxlint` → temiz.

- [ ] **Step 7: Commit**

```bash
git add src/useZoom.ts tests/zoom.test.ts
git commit -m "feat(zoom): lower zoom floor + auto-reframe on growth so zoom-out shows all"
```

---

## Final (whole-branch) verification — orchestrator does this, not a task

1. **Whole-branch review (opus subagent):** spec ile diff'i karşılaştır; regresyon (sıfır-commit özdeşliği), GitHub panel örtüşmesi, translate doğruluğu, zoom taban etkisi.
2. **Browser (controller, elle):** dev server; 12+ commit + 3–4 branch aç → (a) etiket çakışması yok, (b) uzaklaştır tüm geçmişi sığdırıyor, (c) yakınlaştır+kaydır okunur, (d) az-commit görüntü eskisiyle aynı, (e) hem guided hem sandbox modda çalışıyor.
3. **Merge → main → push → canlı:** fast-forward merge, `npm run build` + `npx vitest run` main üstünde yeşil, `git push origin main` (Netlify auto-deploy).

## Self-review (plan vs spec)

- **Coverage:** Sabit boşluk (T2 S2), sceneLayout (T1), GitHub translate (T2 S5/S7), MIN_SCALE (T3 S3), akıllı çerçeveleme (T3 S4), App dinamik genişlik (T2 S9), sıfır-commit özdeşliği (T1 test). ✅
- **Placeholder:** yok. ✅
- **Tip tutarlılığı:** `sceneLayout` alan adları T1 tanımı ↔ T2 kullanımı (`computerPanelW`, `repoBoxW`, `ghPanelW`, `dx`, `sceneW`, `sceneH`) birebir; `GAP`/`SCENE_H` ortak. `GitHubSide` `panelW` prop T2 S4 ↔ S7. ✅
